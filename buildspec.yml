version: 0.2

phases:
  install:
    commands:
      - echo Installing
      - pip install -r requirements.txt || true

  build:
    commands:
      - echo Build started
      - python app.py || echo "No app file"

artifacts:
  files:
    - '**/*'
